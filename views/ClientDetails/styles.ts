import {StyleSheet} from 'react-native'

const styles = StyleSheet.create({
  text: {
    marginBottom: 20,
    fontSize: 18,
    fontWeight: '600',
  },
  subheading: {
    fontWeight: 'normal',
  },
  button: {
    marginTop: 20,
    backgroundColor: 'red',
  },
})

export default styles
